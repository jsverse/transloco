import { ApplicationConfig } from '@angular/core';

// `provideTranslocoTitleStrategy()` is never imported in this project, so
// route titles must not be extracted as translation keys.

export const appConfig: ApplicationConfig = {
  providers: [],
};
