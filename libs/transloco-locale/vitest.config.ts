import { defineAngularProject } from '../../tools/vitest/define-project';

export default defineAngularProject({
  name: 'transloco-locale',
  root: __dirname,
  coverageDir: '../../coverage/libs/transloco-locale',
});
