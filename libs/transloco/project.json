{
  "name": "transloco",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/transloco/src",
  "prefix": "transloco-nx-shell",
  "tags": [],
  "targets": {
    "build-package": {
      "dependsOn": ["copy-readme"],
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/libs/transloco"],
      "options": {
        "project": "libs/transloco/ng-package.json",
        "tsConfig": "libs/transloco/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/transloco/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "nx build-package transloco"
          },
          {
            "command": "npx tsc -p libs/transloco/tsconfig.schematics.json"
          }
        ]
      },
      "outputs": ["{workspaceRoot}/dist/libs/transloco"]
    },
    "test": {
      "dependsOn": ["test-library", "test-schematics"],
      "executor": "nx:noop"
    },
    "test-library": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/libs/transloco"],
      "options": {
        "configFile": "libs/transloco/vitest.config.ts"
      }
    },
    "test-schematics": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/libs/transloco-schematics-spec"],
      "options": {
        "configFile": "libs/transloco/vitest.config.schematics.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "copy-readme": {
      "command": "cp README.md {projectRoot}/README.md",
      "inputs": ["{workspaceRoot}/README.md"],
      "outputs": ["{projectRoot}/README.md"],
      "cache": true
    }
  }
}
