{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsTransloco",
  "title": "Adds Transloco to a project",
  "type": "object",
  "properties": {
    "langs": {
      "description": "The languages of the project.",
      "x-prompt": "\uD83C\uDF0D Which languages do you need?",
      "type": "string",
      "default": "en, es",
      "alias": "la"
    },
    "ssr": {
      "description": "Deprecated: no longer needed. Angular resolves the loader's relative URLs during server side rendering.",
      "type": "boolean",
      "default": false,
      "x-deprecated": "SSR needs no special setup; this option has no effect."
    },
    "loader": {
      "description": "The loader strategy for the translate files.",
      "type": "string",
      "default": "Http",
      "enum": ["Http", "Webpack"],
      "alias": "lo"
    },
    "translateType": {
      "description": "The translation files type.",
      "type": "string",
      "default": "JSON",
      "enum": ["JSON", "Typescript"],
      "alias": "t"
    },
    "path": {
      "type": "string",
      "description": "Translation files path (auto-detects public/ for Angular 18+)",
      "alias": "p"
    },
    "project": {
      "description": "The project name.",
      "type": "string",
      "$default": {
        "$source": "projectName"
      }
    },
    "module": {
      "description": "The root module name.",
      "type": "string",
      "default": "app"
    }
  },
  "required": []
}
