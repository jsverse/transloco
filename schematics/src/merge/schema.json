{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsTranslocoScope",
  "title": "Merge all of the project's translation files into one.",
  "type": "object",
  "properties": {
    "rootTranslationPath": {
      "type": "string",
      "description": "The folder that contain the root translation files.",
      "x-prompt": "Please enter your root translation files path.",
      "default": "src/assets/i18n",
      "alias": "root"
    },
    "outDir": {
      "type": "string",
      "description": "The output directory path.",
      "x-prompt": "Please enter the output directory path.",
      "default": "dist-i18n",
      "alias": "o"
    },
    "project": {
      "type": "string",
      "description": "The root project name."
    }
  },
  "required": []
}
