import { TranslocoListPipe } from './transloco-list.pipe';

describe('TranslocoListPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslocoListPipe();
    expect(pipe).toBeTruthy();
  });
});
